services:
  - type: web
    name: talentsuite-api
    runtime: python
    pythonVersion: "3.11.6"
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    plan: free
